[pytest]
pythonpath = .
markers =
    integration: appels réseau réels (clés en env uniquement)
    postgres: nécessite DATABASE_URL et schéma pgvector appliqué
    slow: charge sentence-transformers (plusieurs secondes)
